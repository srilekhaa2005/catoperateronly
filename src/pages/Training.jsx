import {useEffect,useState} from 'react'; import {BookOpen,ChevronRight} from 'lucide-react'; import {api} from '../api/service'; import {PageHeader} from '../components/Primitives';
export default function Training({language}){const [content,setContent]=useState([]);useEffect(()=>{api.training(undefined,language).then(r=>setContent(r.content))},[language]);return <><PageHeader title="Training Hub" subtitle="Short, in-the-moment operator guidance"/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{content.map(c=><article key={c.id} className="card p-5"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-cat-yellow/10 text-cat-yellow"><BookOpen/></div><h2 className="mt-4 font-bold">{c.title}</h2><p className="mt-2 text-sm leading-6 text-zinc-400">{c.body_text}</p><div className="mt-4 flex items-center justify-between text-xs text-zinc-500"><span>Alert: {c.related_alert_type.replaceAll('_',' ')}</span><ChevronRight size={15}/></div></article>)}</div></>}
